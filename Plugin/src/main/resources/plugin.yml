name: AllGearedUpCore
version: '${project.version}'
main: me.partlysunny.AllGearedUpCore
api-version: 1.16
commands:
  agu:
    aliases: [ allgearedup, agu ]
    description: AllGearedUp base command for ops

permissions:
  agu.admin:
    default: op
    description: Admin commands (from /agu)